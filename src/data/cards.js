/**
 * Math 24 card deck.
 * Each card has: numbers (array of 4), difficulty ('Easy'|'Medium'|'Hard'), optional solution.
 * All cards have integer-only solutions (no fractions or decimals required).
 */
export const CARDS = [];
